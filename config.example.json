{
  "dbPath": "./data/unified-memory.db",
  "claudeExportDir": "/absolute/path/to/your/claude-export-directory",
  "claudeHome": "~/.claude",
  "claudeProjects": [],
  "agents": {
    "claude": { "adapter": "mock", "enabled": true },
    "codex": { "adapter": "mock", "enabled": true }
  },
  "redaction": { "enabled": true },
  "_notes": {
    "adapter": "'mock' runs offline & free (default). Set to 'claude-cli' / 'codex-cli' to invoke the real authenticated CLIs (may incur cost).",
    "model": "Optional `model` field per agent. RECOMMENDED: omit it — each CLI then uses its own default (claude: ~/.claude/settings.json `model`; codex: ~/.codex/config.toml `model`). Explicit codex model ids are often rejected on ChatGPT-account logins (e.g. 'gpt-5-codex' returns 400).",
    "claudeProjects": "Empty = ingest all ~/.claude/projects/* slugs. List slugs (e.g. '-Users-you') to restrict.",
    "dbPath": "Kept under ./data/ which is gitignored — the DB holds raw message content and is a sensitive artifact."
  }
}
